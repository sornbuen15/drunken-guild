# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before making files.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the user is
#   expected to use the image or environment they want.
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if you choose not to, uncomment the following line.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   However, if you decide not to, uncomment the following line.
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm build status files
.pdm-build/

# PIP / Poetry virtual environments
.venv/
venv/
ENV/
env/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# OS metadata
.DS_Store
Thumbs.db

# Editor configurations (VS Code, JetBrains)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Sensitive configurations & Credentials
.env
.env.*
.env-*
!.env.example
.git-credentials
*token*
*credential*
credentials.json
client_secret*.json
*.pem
*.key
*.p12
*.pfx

# Local Agent Configurations and Logs. Antigravity's state lives here and is
# none of our business -- except the two things that are deliberately shared.
# `.agents/*` and not `.agents/`: git does not descend into an ignored
# DIRECTORY, so a negation for a file inside one is never even considered.
.agents/*
# AGENTS.md is Antigravity's copy of the rules Claude reads in CLAUDE.md, so it
# is tracked while the rest of `.agents/` is not. `.agents/skills/` used to be
# carved out here too; it is gone (DG-267) -- skills and agents now have one
# source, in `skills/` and `agents/`, and both are covered by
# scripts/check_ignored_sources.py so a rule like this cannot swallow one
# unnoticed. That happened three times in one session before the guard existed
# (DG-259): gitignore does not affect files already in the index, so existing
# files kept working and only a NEW one was invisible to `git add`.
!.agents/AGENTS.md

# Local Claude Code state (worktrees, scheduled tasks, checkpoints, etc.).
# settings.json is the one exception: it is the shared permission allowlist
# and denylist, so it has to ship with the repo -- a tool other people can
# use safely is the whole point (Boss's rule 4). Personal overrides belong in
# settings.local.json, which stays ignored.
.claude/*
!.claude/settings.json

# Local scratch. Dockerfile used to be listed here as "simulation config, do
# not commit" -- DG-228 made it a deliverable (it is how the pinned install is
# verified), and the stale rule silently kept it out of #114 while the PR
# described it in detail. Found by DG-259's own test failing in CI.
scratch_*.py
not_use/

# Generated by `drunken-config --kind install` from uv.lock. Regenerable,
# and committing it would put a second source of truth beside the lock.
requirements.lock.txt

# Retired code. Kept on disk, deliberately not committed -- DG-291.
# `an agent does not delete` exists so work is not lost; it is not a rule to
# publish. This directory was tracked only because nothing here excluded it, and
# publishing it shipped a drifted duplicate of a live skill, drew Dependabot
# alerts and a merged pull request against withdrawn code, and stood as a second
# archive beside git history. The index that stays committed is RETIRED.md.
_not_used/

# Session scratchpad for cross-agent coordination -- not project record.
# The Boss confirmed this is working state, not history: it belongs to
# whoever's session wrote it, not to git. Anchored to root so it does not
# swallow .guild_templates/SESSION_CHECKPOINT.md, the tracked template this
# file is generated from.
/SESSION_CHECKPOINT.md

# DG-313 / DG-250: operating config, not source. It names this machine
# paths and which project each server serves, so it lives at the wrapper
# level and is generated by scripts/install/install_mcp.sh.
.mcp.json
