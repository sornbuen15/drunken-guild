# macOS
.DS_Store
**/.DS_Store

# Backups
oldversion.zip
*.zip

# Editor
.vscode/
.idea/

# Skill temporary buffer files (auto-deleted after use — never commit)
.claude/temp_*.md

# Local MCP settings (contains machine-specific absolute paths)
.claude/settings.json
.claude/settings.local.json

# Kanban board runtime data (generated at runtime, not source)
.claude/board/
