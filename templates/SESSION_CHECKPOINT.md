# Session checkpoint — <YYYY-MM-DD>

> **This is a template.** Copy it to your project root as `SESSION_CHECKPOINT.md` and add
> `/SESSION_CHECKPOINT.md` to `.gitignore` — it is a session scratchpad, not project record,
> and it belongs to whoever's session wrote it. Authored in `drunken-guild`'s
> `templates/SESSION_CHECKPOINT.md`.
>
> Read it at the start of a session; rewrite it before ending one. **Jira stays the record.**
> Anything here that disagrees with Jira is wrong — this is a handoff note, not a second board.

---

## 1. Waiting on a human — in order

<What only the Boss can do, and in which order: a PR to merge, a decision, a command that is
theirs to run. Name the exact command.>

## 2. Where things stand

<Branches and their heads, what is in flight, anything merged but not deployed.>

## 3. Next

<What the next session should pick up, and what it depends on.>

## 4. Decided — do not reopen

<Decisions already made, who made them, and when. The next session will otherwise re-argue them.>

## 5. Gotchas

<Anything that cost this session time and will cost the next one the same.>
