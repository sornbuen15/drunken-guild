# Agent Index

Map a task to the agent that owns it. Read this before delegating — do NOT guess agent
names or paths from memory.

Generated by `scripts/install/install_agents.sh`. Do not edit by hand; edit the agent
frontmatter instead.

- `manager` (`claude-opus-5`) — The team's manager role. Use to turn a project's requirements into an ordered, approved plan and to dispatch the work — reading the project's documents, breaking work into Epics, Stories and Tasks, de
  Path: $HOME/.claude/agents/manager.md

- `reviewer` (`claude-sonnet-5`) — The team's reviewer role — quality and security in one. Use to review a manager's plan (one round, as comments), to review a worker's tests before implementation starts, and to review a pull request b
  Path: $HOME/.claude/agents/reviewer.md

- `worker` (`claude-sonnet-5`) — The team's worker role. Use to implement exactly one approved task end to end — in its own branch and worktree, test first from the task's acceptance, the smallest change that meets it, the suite gree
  Path: $HOME/.claude/agents/worker.md
